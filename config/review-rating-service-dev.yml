spring:
  application:
    name: review-rating-service

  datasource:
    url: jdbc:mysql://localhost:3306/kodeecom   # changed from mysql → localhost
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka   # changed from eurekaservice
    register-with-eureka: true
    fetch-registry: true

feign:
  hystrix:
    enabled: true
